<?

    // ========================================================================================================================
    // Definition of NetIO230B_Devices
    // ========================================================================================================================
    function get_NetIO230BDevices () {
        $devices = array();
        $devices[] = array(
            v_USERNAME  =>  "username",
            v_PASSWORD  =>  "password",
            v_IP        =>  "192.168.178.123",
            
        );
        
        return $devices;
    }
    
    // ========================================================================================================================
    // Definition of Settings
    // ========================================================================================================================
    function get_NetIO230BSettings() {
        $settings = array(
        );
        
        return $settings;
    }
    
?>